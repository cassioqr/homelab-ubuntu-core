# SSH Setup

## Configurações aplicadas
- Porta: 26111
- Root login: disabled
- Password login: enabled (temporário)
- MFA via PAM (Google Authenticator)
- Keyboard-interactive habilitado

## Motivo
Reduzir ataques automatizados e adicionar camada MFA em acesso por senha.

## Nota
SSH key bypassa MFA por design (config intencional).
